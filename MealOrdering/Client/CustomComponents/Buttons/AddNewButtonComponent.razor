@inherits BaseButtonComponent
<ButtonComponent Caption="@(Caption ?? "Add")"
                 Color="@(Color ?? "primary")"
                 Icon="@(Icon ??"plus-square")"
                 OnClick="@OnClick"
                 ToolTip="@(ToolTip ?? "Add")"
                 IsOutline=@IsOutline
                 IsSmall=@IsSmall />